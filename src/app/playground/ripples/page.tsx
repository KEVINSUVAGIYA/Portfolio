import { RippleCanvas } from "@/components/Playground/Ripples/RippleCanvas";

export default function RipplesPage() {
    return (
        <main className="w-full h-screen overflow-hidden bg-slate-950">
            <RippleCanvas />
        </main>
    );
}
